static class GLOBALS {
  static boolean PAUSED = false;
  static float FORCE_MAGNITUDE = 1.3;
  static int STEPS_PER_GENERATION = 120;
  static int POPULLATION_SIZE = 5000;
  static float MUTATION_RATE = 0.05;  // Percentage of childs to mutate on each iteration
  static float VELOCITY_LIMIT = 8.6f;
}